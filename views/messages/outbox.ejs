<div class="inbox-container">
  <h1>Your Sent Messages (Outbox)</h1>

 <% if (conversations.length === 0) { %>
  <p>You haven't sent any messages yet.</p>
<% } else { %>
  <% conversations.forEach(conv => { %>
    <a href="/messages/thread/<%= conv.original._id %>" class="message-link">
      <div class="message-card">
        <div class="sender-info">
          <strong>To: <%= conv.receiverUsername %></strong>
          <small><%= new Date(conv.latest.createdAt).toLocaleString() %></small>
        </div>
        <p><%= conv.latest.text %></p>
      </div>
    </a>
  <% }) %>
<% } %>

  <div class="public-link">
    <p>Your public link:</p>
    <code><%= publicLink %></code>
    <button onclick="copyLink()" class="copy-btn">Copy</button>
  </div>
</div>